<UserControl x:Class="Nabu.Undo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Nabu"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Grid.Column="2" Width="45" Height="45">
            <Button.Template>
                <ControlTemplate>
                    <Border CornerRadius="50" Background="#dedddb">
                        <materialDesign:PackIcon Background="{x:Null}" Kind="Home" Width="50" Height="50" Foreground="#f0eff1"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
